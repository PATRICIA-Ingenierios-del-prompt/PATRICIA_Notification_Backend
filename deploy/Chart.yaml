apiVersion: v2
name: notification
description: PATRICIA Notification MS (8091, WebSocket/STOMP) — deployed by the CD job in .github/workflows/CI.yml on push to main
type: application
version: 0.1.0
appVersion: "0.0.1"
